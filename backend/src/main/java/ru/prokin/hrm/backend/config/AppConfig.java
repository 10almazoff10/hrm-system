package ru.prokin.hrm.backend.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class AppConfig {
    // Configuration class kept for future use if needed
    // Currently empty as all beans are defined in SecurityConfig
}